<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.achmadfarhanfebrianto_1202154208_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"> <!-- untuk menghilangkan nav bar -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Splash screen -->
    <activity android:name=".Menu"/>
    <activity android:name=".takeaway" />
        <activity android:name=".detailmakanan"
            android:parentActivityName="com.example.android.achmadfarhanfebrianto_1202154208_modul2.detailmenu">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".detailmenu"/>


        </activity>
        <activity android:name=".dinein" />
        <activity android:name=".IsiMenu" />
        <activity android:name=".detailmenu"
           >

        </activity>
    </application>

</manifest>